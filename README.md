# Email-Generator-Java
 Basic java backend work to generate email & password for the user and store that to a text file. User can also update email or password and read the stored data from the text file.
